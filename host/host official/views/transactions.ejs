<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0">
    <title>
        Abang - Provider
    </title>
    <% include snips/reg-header %>
</head>

<body class="is-loading">
<div class="wrapper">
    <% include snips/log-nav %>
    <div class="row justify-content-center margin-top">
        <div class="container justify-content-center m-5 pb-5">
            <div class="card">
                <div class="card-header">
                    <h1><%= title %> (<%= data.length %>)</h1>
                </div>
                <% if( data.length > 0) { %>
                <div class="card-body">
                    <table>
                        <tr>
                            <td>Date</td>
                            <td>Price</td>
                        </tr>
                        <% data.forEach(function(list) { %>
                        <tr>
                            <td><%= list.trandate.toDateString() %></td>
                            <td><%= list.amount %></td>
                        </tr>
                        <% }); %>
                    </table>
                </div>
                <% } else { %>
                <div class="card-body">
                    <p>Hm, you don't have any transactions yet. You must be new here! Remember that this is your monthly fee based on all your sucessful listings!</p>
                </div>
                <% } %>
            </div>
        </div>
    </div>
</div>
</body>

</html>